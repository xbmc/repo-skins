<?xml version="1.0" encoding="UTF-8"?>
<window type="dialog" id="1118">
	<onunload condition="!Player.HasVideo + Skin.HasSetting(BGsoundOff) + Player.Muted ">Mute</onunload>
	
	<visible>Player.HasVideo + Skin.HasSetting(BGsoundOff) + ![VideoPlayer.IsFullscreen + Window.IsVisible(fullscreenvideo)] + ![Window.IsVisible(MyPVRChannels.xml) | Window.IsVisible(MyPVRRecordings.xml) | Window.IsVisible(MyPVRTimers.xml) | Window.IsVisible(MyPVRSearch.xml)]</visible>
	<controls></controls>
</window>