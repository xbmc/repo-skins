<?xml version="1.0" encoding="UTF-8"?>
<window>
	<description>
		Window for game functionality. This dialog was originally for controllers,
		but adding new XML windows breaks old skins, so it has been repurposed
		for any game-related windows.
	</description>
	<defaultcontrol always="true">3</defaultcontrol>
	<controls>
        	<control type="group" id="9999">
			<include condition="Window.IsDialogTopMost(gamecontrollers)">DialogGameControllers</include>
			<include condition="Window.IsDialogTopMost(gameports)">DialogGamePorts</include>
			<include condition="Window.IsDialogTopMost(gameagents)">DialogGameAgents</include>
            	</control>
    </controls>
</window>
